@model SecureVault.Models.Vaults

@{
    ViewBag.Title = "Secure Vault - Vault Details";
}

<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Vault Details</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <div>
        <!--<div class="btn-group btn-group-justified" role="group" aria-label="...">
            <div class="btn-group" role="group">
                <button type="button" class="btn btn-default" data-toggle="modal" data-target="#editVault">Edit Vault</button>
            </div>
            <div class="btn-group" role="group">
                <button type="button" class="btn btn-default">Merge Vault</button>
            </div>
            <div class="btn-group" role="group">
                <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteVaultCheck">Delete Vault</button>
            </div>
        </div>-->
        <br />

        <div class="panel-group" id="accordion">
            @foreach (var key in Model.Keys)
            {
                <div class="panel panel-default">
                    <a data-toggle="collapse" data-parent="#accordion" href="#collapse@(key.Key)">
                        <div class="panel-heading">
                            <h4 class="panel-title">

                                @key.Key
                            </h4>
                        </div>
                    </a>
                    <div id="collapse@(key.Key)" class="panel-collapse collapse">
                        <div class="panel-body">
                            <p>@key.Value</p>
                            <p>@key.DateCreated</p>
                            <p>@key.Description</p>
                        </div>
                        <div class="btn-group btn-group-justified" role="group" aria-label="...">
                            <div class="btn-group" role="group">
                                <button type="button" class="btn btn-default" style="border-top-left-radius:0;">Edit Key</button>
                            </div>
                            <div class="btn-group" role="group">
                                <button type="button" class="btn btn-default">Move Key</button>
                            </div>
                            <div class="btn-group" role="group">
                                <button type="button" class="btn btn-danger"  style="border-top-right-radius:0;">Delete Key</button>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>

@*        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DateCreated)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DateCreated)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Users.Email)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Users.Email)
            </dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
*@
    </div>
</div>